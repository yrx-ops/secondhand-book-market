---
description: 二手图书交易平台全局开发规范
alwaysApply: true
---

# Project Overview

这是一个前后端分离的校园二手图书交易平台。

项目目标是作为计算机专业毕业生的求职作品，
代码需要兼顾可运行性、规范性、可维护性和面试展示价值。

# Tech Stack

## Backend

- Java 21
- Spring Boot 3
- Maven
- Spring MVC
- Spring Security
- JWT
- MyBatis-Plus
- Hibernate Validator
- MySQL 8

## Frontend

- Vue 3
- TypeScript
- Vite
- Vue Router
- Pinia
- Axios
- Element Plus

# Project Structure

项目采用前后端分离结构：

- backend/：Spring Boot 后端
- frontend/：Vue 前端
- docs/：项目文档

不要把前端和后端代码混在同一个目录中。

# Backend Architecture Rules

后端按照职责分层：

controller
service
service/impl
mapper
entity
dto
vo
config
security
exception
common

要求：

- Controller 只负责接收请求、参数校验和返回响应。
- Controller 不直接操作数据库。
- 业务逻辑写在 Service 层。
- Mapper 只负责数据库访问。
- Entity 对应数据库表。
- DTO 用于接收前端参数。
- VO 用于返回前端数据。
- 不直接把 Entity 作为所有接口返回值。
- 公共响应结构统一管理。
- 全局异常统一处理。

# Java Coding Rules

- 使用 Java 21。
- 类名使用 PascalCase。
- 方法名和变量名使用 camelCase。
- 常量使用 UPPER_SNAKE_CASE。
- 数据库字段使用 snake_case。
- 不在代码中写魔法数字和魔法字符串。
- 一个方法只处理一个清晰职责。
- 避免超长方法和超大类。
- 核心业务逻辑必须添加必要注释。
- 不要为了减少代码而牺牲可读性。
- 不修改与当前任务无关的代码。

# API Rules

REST API 统一使用：

/api/v1

例如：

GET /api/v1/books
GET /api/v1/books/{id}
POST /api/v1/books
PUT /api/v1/books/{id}
DELETE /api/v1/books/{id}

HTTP 方法正确表达业务语义：

- GET：查询
- POST：创建
- PUT/PATCH：修改
- DELETE：删除

接口返回使用统一响应结构。

# Validation Rules

所有来自前端的重要参数必须进行服务端校验。

使用：

Hibernate Validator

例如：

@NotBlank
@NotNull
@Size
@Min
@Max
@Email

不能只依赖 Vue 前端校验。

# Security Rules

- 密码不能明文保存。
- 密码使用 BCrypt 加密。
- 登录使用 Spring Security + JWT。
- 不信任前端传入的 userId。
- 当前登录用户身份必须从认证信息中获取。
- 用户不能修改其他用户的数据。
- 用户不能编辑或删除别人发布的图书。
- 用户不能查看无权限访问的数据。
- 数据库密码、JWT Secret 等敏感配置不能提交到 GitHub。
- 不把敏感配置硬编码到 Java 或 Vue 文件中。

# Database Rules

数据库使用 MySQL 8。

数据库表名和字段名统一使用 snake_case。

每张核心业务表应根据实际情况包含：

- id
- created_at
- updated_at

金额字段使用：

DECIMAL

不要使用：

FLOAT
DOUBLE

存储金额。

需要查询的字段根据业务建立索引。

数据库结构修改必须同步更新 SQL 文档。

# Frontend Architecture Rules

Vue 使用 Composition API。

优先使用：

<script setup lang="ts">

项目目录按照职责拆分：

src/
├── api/
├── assets/
├── components/
├── layouts/
├── router/
├── stores/
├── types/
├── utils/
└── views/

要求：

- 页面放在 views。
- 可复用组件放在 components。
- HTTP 请求统一放在 api。
- Pinia Store 放在 stores。
- TypeScript 类型放在 types。
- 工具函数放在 utils。
- 不要在 Vue 页面中直接写大量 Axios 请求。

# Frontend Coding Rules

- 使用 TypeScript。
- 不随意使用 any。
- 使用 Vue 3 Composition API。
- 优先使用 <script setup lang="ts">。
- 可复用逻辑应拆成 composable 或工具函数。
- 页面组件不要过大。
- 表单必须进行前端校验。
- 请求必须处理 loading、error 和 empty 状态。
- 用户操作需要提供明确的成功或失败提示。

# Business Rules

- 每本二手图书默认只有一件库存。
- 用户不能购买自己发布的图书。
- 只有图书发布者可以编辑或下架自己的图书。
- 只有 AVAILABLE 状态的图书可以购买。
- 同一本二手书不能同时产生两个有效订单。
- 下单时必须在服务端重新检查图书状态。
- 订单金额必须从数据库读取。
- 不允许相信前端传入的商品价格。
- 订单必须保存下单时的图书标题和价格快照。
- 已售出的图书不能再次购买。
- 已完成订单不能随意修改回之前状态。

# Workflow Rules

在修改代码前：

1. 先阅读相关代码。
2. 说明准备修改哪些文件。
3. 说明实现方案。
4. 不修改与当前任务无关的内容。

修改完成后：

后端至少检查：

mvn test

或：

mvn clean package

前端至少检查：

npm run build

如果出现错误：

- 先分析根本原因。
- 不为了消除错误而随意删除代码。
- 不随意降低类型检查和安全检查。
- 不未经说明安装新的依赖。

# AI Rules

Cursor 在执行较大的任务时：

1. 先分析。
2. 先给计划。
3. 再修改代码。
4. 每次只完成一个明确的小功能。
5. 完成后说明修改了哪些文件。
6. 给出人工测试步骤。
7. 不擅自扩大需求范围。
